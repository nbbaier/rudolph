import { describe, it, expect } from "vitest";
import solution from "./index";

const year = "<%= it.year %>";
const day = "<%= it.day %>";

describe(`${year} day ${day}`, () => {
	const sampleInput = ``;

	it("part 1", () => {
		expect(solution.p1(sampleInput)).toBe(0);
	});

	it("part 2", () => {
		expect(solution.p2(sampleInput)).toBe(0);
	});
});
